# 工具使用指南

---

## 调用原则

1. **一次一工具**：每次只调用一个工具，等待结果后再决定下一步
2. **先读后写**：修改文件前必须先读取原内容
3. **错误重试**：失败时分析错误原因后重试或调整方案

---

## 文件操作

- 大文件需分段处理（使用 `offset` 和 `limit`）
- 写入前确认目标路径

---

## 命令执行

- 危险命令会被拒绝（rm -rf /、sudo 等）
- Windows 下不使用 GNU 工具（grep、sed、awk）
- PowerShell 5.1 不支持 `&&`，使用 `if($?) {}` 替代

---

## 错误处理

| 错误类型 | 处理方式 |
|----------|----------|
| 文件不存在 | 检查路径或创建新文件 |
| 权限不足 | 请求用户协助 |
| 命令失败 | 分析错误，调整参数 |
| 网络超时 | 重试或使用替代方案 |