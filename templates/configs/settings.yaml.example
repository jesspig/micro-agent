# ─────────────────────────────────────────────────────────────────
# MicroBot 配置文件示例
# ─────────────────────────────────────────────────────────────────
#
# 复制此文件到以下位置：
#   - 用户级：~/.microbot/settings.yaml（全局配置）
#   - 项目级：<workspace>/.microbot/settings.yaml（项目配置）
#   - 目录级：<path>/.microbot/settings.yaml（目录配置）
#
# 配置合并顺序（后者覆盖前者）：
#   用户级 < 项目级 < 目录级
#
# 环境变量：支持 ${VAR_NAME} 语法
#
# ─────────────────────────────────────────────────────────────────

agents:
  workspace: ~/.microbot/workspace

  # 模型配置（必填）
  # models:
  #   chat: ollama/qwen3
  #   check: ollama/qwen3

  # 生成参数
  # maxTokens: 8192
  # temperature: 0.7
  # topK: 50
  # topP: 0.7
  # frequencyPenalty: 0.5
  # maxToolIterations: 20

  # 自动路由
  # auto: true
  # max: false

# Provider 配置
# providers:
#   ollama:
#     baseUrl: http://localhost:11434/v1
#     models:
#       - qwen3
#
#   deepseek:
#     baseUrl: https://api.deepseek.com/v1
#     apiKey: ${DEEPSEEK_API_KEY}
#     models:
#       - id: deepseek-chat
#         level: medium

# Channel 配置
# channels:
#   feishu:
#     enabled: true
#     appId: your-app-id
#     appSecret: your-app-secret
#     allowFrom: []

# Routing 配置
# routing:
#   enabled: true
#   rules: []
